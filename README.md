# homebrew-whisperdrop

Homebrew tap for [WhisperDrop](https://github.com/kridsadar357/whisperdrop).

```bash
brew tap kridsadar357/whisperdrop
brew install whisperdrop
```

The formula is regenerated by the release workflow of the main repository
(`scripts/gen-formula.sh`) on every tagged release.
